# sort ascending numbers ( ascending order)
numbers = [5, 2, 8, 1, 9]
numbers.sort() # Changes the original list
print(numbers) # [1, 2, 5. 8. 9]

# or create  sorted list

numbers = [5, 2, 8, 1, 9]
sorted_numbers = sorted(numbers) # Returns new list
print(sorted_numbers) # [1,2, 5, 8, 9]

fruits = ["banana", "apple", "cherry"]
fruits.sort()
print(fruits) # ['apple', 'banana', 'cheery']

mobile = ['Iphone', 'Apple', 'motorola']
mobile.sort()
print(mobile)

# descending order
numbers = [5, 2, 8, 1, 9]
numbers.sort(reverse=True)
print(numbers)

sorted_numbers =sorted(numbers, reverse=True)
print(sorted_numbers)

# bubble sort
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        # Last i element are already in place
        for j in range(0, n- i -1):
            if arr[j] > arr[j+1]:
                # Swape if wrong order
                arr[j], arr[j+1] = arr[j+1], arr[j]
    return arr

numbers = [5, 2, 8, 1, 9]
print('Bubble sort result:', bubble_sort(numbers.copy()))

# selection Sort 
def selection_sort(arr):
    n = len(arr)
    for i in range(n):
        min_idx = i
        for j in range(i + 1, n):
            if arr[j] < arr[min_idx]:
                min_idx = j
        
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    return arr
    
numbers = [5, 2, 8, 1, 9]
print("Selection sort result:", selection_sort(numbers.copy()))

# SOrted by lenghth
fruits = ["banana", "apple", "cherry", "kiwi"]
fruits.sort(key=len) # Sort by length
print(fruits)

# sorted by second element in tuple
students = [("Alice", 85), ("Bob", 92), ("Charlie", 78)]
students.sort(key=lambda x: x[1]) 
print(students)

# case-insensitive sorting

names = ["alice", "Chajrles", "Bob", "David"]
names.sort(key=str.lower) # Ignore case
print(names) 
