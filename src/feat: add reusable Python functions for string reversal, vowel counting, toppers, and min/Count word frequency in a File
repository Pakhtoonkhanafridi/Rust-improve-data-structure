# Count Word Frequency in a File
def count_word_frequency(filename):
    """
    COunt how many times each word appears  in a file .
    Returns a dictionary with word frequencies
    """
    word_count = {}
    
    try:
        with open(filename, "r") as file:
            for line in file:
                # Convert to lowercase and split into words
                words = line.lower().split()
                
                for word in words:
                    # Remove punction from words
                    clean_word = word.stript('.,!?;;"()')
                    
                    if clean_word:
                        if clean_word in word_count:
                            word_count[clean_word] += 1
                        else:
                            word_count[clean_word] =1
            
            return word_count
    except FileNotFoundError:
        print(f"Error: The file  '{filename} was not found.")
        return {}

def display_word_frequency(word_count):
    """
    Displays the word frequency count in a nice format.
    """
    if not word_count:
        print("No words to display.")
        return
    
    print("\nWord Frequency Count:")
    print("-" * 25)
    
    sorted_words = sorted(word_count.items(), key= lambda x: x [1], reverse = True)
    for word, count in sorted_words:
        print(f"{word}: {count}")
        
def create_sample_file():
    """ Create a sample text file for testing. """
    sample_text = """ Hello world! This is a test file .
    hello again , this is Python programming.
    Hello python!"""
    
     with open("sample.txt", "w") as file:
         file.write(sample_text)
     print("Created sample.txt for testing.")
     
create_sample_file() 
word_counts = count_word_frequency("sample.txt")
display_word_frequency(word_counts)
